export declare namespace Post_User_Create {
    export type Query = {
        readonly hello: string;
    };
    export type Body = {
        readonly world: string;
    };
    export type Response = {
        readonly res: string;
    };
}
